"use client";
import type { PortableTextComponentProps } from "@portabletext/react";

export default function ImageComponent({
  value,
  isInline,
}: PortableTextComponentProps<string>): React.ReactElement {
  return <h1>Img huh?</h1>;
}
